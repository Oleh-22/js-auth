<div class="field">
	<label form="{{name}}" class="field__label">{{label}} </label>
	<input oninput='{{action}}(this.name, this.value)' type="{{type}}" class="field__input validation" name="{{name}}"
		placeholder="{{placeholder}}">
</div>